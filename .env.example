# ============================
# CUI-IP-ID environment config
# ============================

# App mode:
#   demo = no LLM, offline deterministic demo
#   real = LLM-backed analysis (not enabled yet)
APP_MODE=demo

# --- REQUIRED for real mode only ---
OPENAI_API_KEY=sk-REPLACE_ME

# --- OPTIONAL: usage logging (leave blank to disable) ---
USAGE_LOG_URL=
USAGE_LOG_TOKEN=
